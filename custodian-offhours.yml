policies:
  - name: offhours-stop
    mode:
      type: periodic
      schedule: rate(1 hour)
      role: << CloudCustodianRoleForOffHours >>
      execution-options:
        metrics_enabled: true
        log_group: /cloud_custodian/offhours
    resource: ec2
    filters:
      - type: offhour
        offhour: 20
        tag: AvailabilitySchedule
        default_tz: gmt
    actions:
      - stop

  - name: offhours-start
    mode:
      type: periodic
      schedule: rate(1 hour)
      role: << CloudCustodianRoleForOffHours >>
      execution-options:
        metrics_enabled: true
        log_group: /cloud_custodian/offhours
    resource: ec2
    filters:
      - type: onhour
        onhour: 8
        tag: AvailabilitySchedule
        default_tz: gmt
    actions:
      - start
